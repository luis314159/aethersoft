export interface Service {
    title: string;
    description: string;
    icon: string;
  }
  
  export interface PortfolioItem {
    title: string;
    imageUrl: string;
    description: string;
  }
  